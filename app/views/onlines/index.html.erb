<h1>Listado de lineas</h1>

<table>
  <thead>
    <tr>
      <th>Numero</th>
      <th>Numero de maquinas</th>
      <th>Numero de operarias</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @onlines.each do |online| %>
      <tr>
        <td><%= online.number %></td>
        <td><%= online.number_machines %></td>
        <td><%= online.number_operator %></td>
        <td><%= link_to 'Ver', online %></td>
        <td><%= link_to 'Editar', edit_online_path(online) %></td>
        <td><%= link_to 'Eliminar', online, method: :delete, data: { confirm: 'Esta seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva linea', new_online_path %>
